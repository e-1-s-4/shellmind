[package]
name = "shellmind-daemon"
description = "Resident shellmind daemon: warm completion cache, incremental history indexing, background embeddings"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
shellmind-core = { path = "../core" }
serde_json.workspace = true
